<div class="center2">
    <p class="menu-title">{{restaurant.name}}</p>
    <div class="menu-cont">
        <div class="menu-item">
            <div>
                <img class="menu-image" src="{{restaurant.image}}">
                <div>
                    {{#each restaurant.menus}}
                        <div class="single-menu-cont">
                        <p class="menu-title2">{{this.title}}</p>
                        {{#each this.items}}
                            <p class="menu-item-text">{{this.name}} <br><b>£{{this.price}}</b></p>
                        {{/each}}
                        </div>
                    {{/each}}
                </div>
            </div>          
        </div>
    </div>
</div>

<script>
    $("#container").css('visibility', 'visible');
    $("#container").hide();
    $("#container").slideDown(800)
</script>
